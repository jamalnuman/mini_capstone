
  # product: @products
  @products.map do |product| #there is no need to include the curly brackets to include the previous code. 
    {
      id: product.id,
      name: product.name, 
      price: product.price, 
      image_url: product.image_url,
      description: product.description
    }
  end
